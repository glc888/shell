name: Build Controller EXE
on:
  workflow_dispatch:   # ✅网页点按钮手动构建
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: PyInstaller build exe (DEBUG模式，带控制台窗口)
        run: |
          # 删掉 --windowed，开启控制台输出print日志
          pyinstaller --onefile --name=Controller controller.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Controller-exe
          path: dist/Controller.exe
